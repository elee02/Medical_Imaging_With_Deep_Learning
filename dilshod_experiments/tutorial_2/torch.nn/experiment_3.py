''' The Data '''

import torch.utils.data as data
# The data package defines two classes which are 
# the standard interface for handling data in PyTorch

# data.Dataset:
# provides an uniform interface to access the training/test data

# data.DataLoader:
# efficiently load and stack the data points from the dataset into batches during training
